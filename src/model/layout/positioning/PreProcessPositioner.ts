import { LayerGraph, Vertex } from "@/model/ds";

export function undoShift(yPos: Map<Vertex, number>, g: LayerGraph, vertBundeling: Map<Vertex, number>): Map<Vertex, number> {
  
  
  
  return undefined;
}
